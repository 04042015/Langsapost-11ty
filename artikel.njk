---
layout: base.njk
title: Semua Artikel - LangsaPost
permalink: /artikel/
---

<main class="container mx-auto px-4 py-8">
    <h1 class="text-3xl font-bold mb-8">Semua Artikel</h1>

    {% if collections.artikel.length > 0 %}
        <div class="space-y-6">
            {% for artikel in collections.artikel %}
                <article class="bg-white rounded-lg shadow-md p-6">
                    <div class="flex flex-col md:flex-row gap-6">
                        {% if artikel.data.image %}
                            <div class="md:w-1/3">
                                <img src="{{ artikel.data.image }}" alt="{{ artikel.data.title }}" class="w-full h-48 object-cover rounded">
                            </div>
                        {% endif %}
                        
                        <div class="flex-1">
                            <h2 class="text-2xl font-semibold mb-3">
                                <a href="{{ artikel.url }}" class="hover:text-blue-600">
                                    {{ artikel.data.title }}
                                </a>
                            </h2>
                            
                            <p class="text-gray-600 mb-4">
                                {{ artikel.templateContent | excerpt }}
                            </p>
                            
                            <div class="flex justify-between items-center text-sm text-gray-500">
                                <span>{{ artikel.data.date | dateFormat }}</span>
                                {% if artikel.data.kategori %}
                                    <span class="bg-blue-100 text-blue-800 px-2 py-1 rounded">
                                        {{ artikel.data.kategori }}
                                    </span>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                </article>
            {% endfor %}
        </div>
    {% else %}
        <p class="text-center text-gray-500 py-8">Belum ada artikel yang dipublikasikan.</p>
    {% endif %}
</main>
