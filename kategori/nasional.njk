---
layout: layouts/base.njk
title: Berita Nasional - LangsaPost
description: Kumpulan berita nasional terkini dari seluruh Indonesia
permalink: /kategori/nasional/
---

<main class="container mx-auto px-4 py-8">
  <div class="mb-8">
    <nav class="text-sm text-gray-500 mb-4">
      <a href="/" class="hover:text-primary-600">Beranda</a>
      <span class="mx-2">›</span>
      <a href="/kategori/" class="hover:text-primary-600">Kategori</a>
      <span class="mx-2">›</span>
      <span class="text-gray-700">Nasional</span>
    </nav>
    
    <div class="flex items-center mb-6">
      <div class="bg-red-100 p-3 rounded-full mr-4">
        <svg class="w-8 h-8 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 21v-4m0 0V5a2 2 0 012-2h6.5l1 1H21l-3 6 3 6h-8.5l-1-1H5a2 2 0 00-2 2zm9-13.5V9"></path>
        </svg>
      </div>
      <div>
        <h1 class="text-4xl font-bold text-gray-900">Berita Nasional</h1>
        <p class="text-xl text-gray-600 mt-2">Berita terkini dari seluruh Indonesia</p>
      </div>
    </div>
  </div>

  {% set nasionalArticles = collections.artikel | filterByCategory("Nasional") %}
  
  {% if nasionalArticles.length > 0 %}
    <!-- Featured Article -->
    {% if nasionalArticles[0] %}
    <section class="mb-12">
      <div class="bg-white rounded-lg shadow-lg overflow-hidden">
        <div class="md:flex">
          {% if nasionalArticles[0].data.image %}
          <div class="md:w-1/2">
            <img src="{{ nasionalArticles[0].data.image }}" alt="{{ nasionalArticles[0].data.title }}" class="w-full h-64 md:h-full object-cover">
          </div>
          {% endif %}
          <div class="p-8 {% if nasionalArticles[0].data.image %}md:w-1/2{% endif %}">
            <div class="flex items-center mb-4">
              <span class="bg-red-100 text-red-800 px-3 py-1 rounded-full text-sm font-medium">Utama</span>
              <span class="ml-3 text-gray-500 text-sm">{{ nasionalArticles[0].data.date | dateFormat }}</span>
            </div>
            <h2 class="text-2xl md:text-3xl font-bold text-gray-900 mb-4">
              <a href="{{ nasionalArticles[0].url }}" class="hover:text-primary-600 transition-colors">
                {{ nasionalArticles[0].data.title }}
              </a>
            </h2>
            {% if nasionalArticles[0].data.description %}
            <p class="text-gray-600 mb-6">{{ nasionalArticles[0].data.description }}</p>
            {% else %}
            <p class="text-gray-600 mb-6">{{ nasionalArticles[0].templateContent | excerpt }}</p>
            {% endif %}
            <a href="{{ nasionalArticles[0].url }}" class="inline-flex items-center text-primary-600 hover:text-primary-800 font-medium">
              Baca Selengkapnya
              <svg class="ml-2 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
              </svg>
            </a>
          </div>
        </div>
      </div>
    </section>
    {% endif %}

    <!-- Articles Grid -->
    <section>
      <h2 class="text-2xl font-bold text-gray-900 mb-8">Semua Berita Nasional</h2>
      <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
        {% for artikel in nasionalArticles %}
        {% if loop.index > 1 %}
        <article class="bg-white rounded-lg shadow-md overflow-hidden hover:shadow-lg transition-shadow">
          {% if artikel.data.image %}
          <img src="{{ artikel.data.image }}" alt="{{ artikel.data.title }}" class="w-full h-48 object-cover">
          {% endif %}
          
          <div class="p-6">
            <h3 class="text-lg font-semibold mb-3">
              <a href="{{ artikel.url }}" class="hover:text-primary-600 transition-colors line-clamp-2">
                {{ artikel.data.title }}
              </a>
            </h3>
            
            {% if artikel.data.description %}
            <p class="text-gray-600 mb-4 text-sm line-clamp-3">
              {{ artikel.data.description }}
            </p>
            {% else %}
            <p class="text-gray-600 mb-4 text-sm line-clamp-3">
              {{ artikel.templateContent | excerpt }}
            </p>
            {% endif %}
            
            <div class="flex justify-between items-center text-sm text-gray-500">
              <span>{{ artikel.data.date | dateFormat }}</span>
              <span class="bg-red-100 text-red-800 px-2 py-1 rounded text-xs">Nasional</span>
            </div>
          </div>
        </article>
        {% endif %}
        {% endfor %}
      </div>
    </section>
  {% else %}
    <div class="text-center py-16">
      <div class="bg-gray-100 rounded-full p-6 w-24 h-24 mx-auto mb-6 flex items-center justify-center">
        <svg class="w-12 h-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 20H5a2 2 0 01-2-2V6a2 2 0 012-2h10a2 2 0 012 2v1m2 13a2 2 0 01-2-2V7m2 13a2 2 0 002-2V9.5a2 2 0 00-2-2h-2m-4-3H9M7 16h6M7 8h6v4H7V8z"></path>
        </svg>
      </div>
      <h3 class="text-xl font-semibold text-gray-900 mb-2">Belum Ada Berita Nasional</h3>
      <p class="text-gray-600 mb-6">Berita nasional akan segera hadir. Silakan kembali lagi nanti.</p>
      <a href="/artikel/" class="inline-flex items-center bg-primary-600 text-white px-6 py-3 rounded-lg hover:bg-primary-700 transition-colors">
        Lihat Semua Artikel
        <svg class="ml-2 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
        </svg>
      </a>
    </div>
  {% endif %}
</main>
