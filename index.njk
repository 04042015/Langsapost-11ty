<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LangsaPost - Portal Berita Terpercaya</title>
    <meta name="description" content="Portal berita terpercaya LangsaPost">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50">
    <header class="bg-white shadow-sm">
        <nav class="container mx-auto px-4 py-4">
            <div class="flex justify-between items-center">
                <a href="/" class="text-2xl font-bold text-blue-600">LangsaPost</a>
                <ul class="hidden md:flex space-x-6">
                    <li><a href="/" class="hover:text-blue-600">Beranda</a></li>
                    <li><a href="/artikel/" class="hover:text-blue-600">Artikel</a></li>
                </ul>
            </div>
        </nav>
    </header>

    <main class="container mx-auto px-4 py-8">
        <section class="hero mb-12 text-center">
            <h1 class="text-4xl font-bold mb-4">LangsaPost</h1>
            <p class="text-xl text-gray-600">Portal Berita Terpercaya</p>
        </section>

        <section class="latest-news">
            <h2 class="text-2xl font-bold mb-6">Berita Terbaru</h2>
            
            {% if collections.artikel.length > 0 %}
                <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
                    {% for artikel in collections.artikel | limit(6) %}
                        <article class="bg-white rounded-lg shadow-md overflow-hidden">
                            {% if artikel.data.image %}
                                <img src="{{ artikel.data.image }}" alt="{{ artikel.data.title }}" class="w-full h-48 object-cover">
                            {% endif %}
                            
                            <div class="p-6">
                                <h3 class="text-xl font-semibold mb-2">
                                    <a href="{{ artikel.url }}" class="hover:text-blue-600">
                                        {{ artikel.data.title }}
                                    </a>
                                </h3>
                                
                                <p class="text-gray-600 mb-4">
                                    {{ artikel.templateContent | excerpt }}
                                </p>
                                
                                <div class="flex justify-between items-center text-sm text-gray-500">
                                    <span>{{ artikel.data.date | dateFormat }}</span>
                                    {% if artikel.data.kategori %}
                                        <span class="bg-blue-100 text-blue-800 px-2 py-1 rounded">
                                            {{ artikel.data.kategori }}
                                        </span>
                                    {% endif %}
                                </div>
                            </div>
                        </article>
                    {% endfor %}
                </div>
            {% else %}
                <p class="text-center text-gray-500 py-8">Belum ada artikel yang dipublikasikan.</p>
            {% endif %}
        </section>

        {% if collections.artikel.length > 6 %}
        <section class="all-articles mt-12 text-center">
            <a href="/artikel/" class="bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700">
                Lihat Semua Artikel
            </a>
        </section>
        {% endif %}
    </main>

    <footer class="bg-gray-800 text-white py-8 mt-12">
        <div class="container mx-auto px-4 text-center">
            <p>&copy; 2024 LangsaPost. Semua hak dilindungi.</p>
        </div>
    </footer>
</body>
</html>
